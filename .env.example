# Base de datos PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/salonbelleza?schema=public"

# JWT - OBLIGATORIO en producción. Generar con: openssl rand -base64 32
JWT_SECRET="CHANGE_ME_IN_PRODUCTION"
